{
  "name": "earlymusicsa",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Static rebuild of earlymusicsa.org — no WordPress, no EventON.",
  "scripts": {
    "scrape": "node scrape.js",
    "scrape:images": "node scrape.js --images",
    "build": "node build.js",
    "start": "node server.js",
    "test": "node test-db.mjs && node test-deploy.mjs && node test-edits.mjs",
    "audit": "node audit.js"
  },
  "engines": {
    "node": ">=18"
  }
}
